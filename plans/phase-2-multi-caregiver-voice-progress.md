# Phase Progress: phase-2-multi-caregiver-voice

**Plan:** `plans/phase-2-multi-caregiver-voice.md`
**Started:** 2026-05-18
**Execution mode:** 4 single-pass tasks, no pre-emptive split (user-decided). `⟂ SPLIT` markers are a recovery fallback only.
**Hard gate:** Task 1 `pnpm test:integration` must be green before Task 2. RLS isolation failure ⇒ Phase 1 regression ⇒ STOP + escalate.

## Task Progress

- [x] Task 1: Pre-flight gate — integration harness + P0 RLS isolation — **COMPLETED** (commit `c16655e`, branch `phase-2-task-1-integration-harness`). Gate GREEN: `test:integration` 12 passed (8 RLS R1 + 4 idempotency); unit 57/DB-free; no Phase 1 regression.
- [x] Task 2: Caregiver identity — invites, peer-recovery, recovery codes, settings, audit wiring — **COMPLETED** (commit `7fcfe3b`). `test:integration` 23/23 (incl. R10 peer-recovery 6 + recovery-code 5); unit 57; §11.5 verbatim + fail-safe; service-role cap intact; `0002_rate_limits.sql` applied.

### Orchestration notes
- **Branch strategy:** all Phase 2 tasks commit on `phase-2-task-1-integration-harness` (de-facto phase branch) so the dependency chain (T2 needs T1 harness; T3 needs T2 settings; T4 needs T2 audit) is satisfied. `main` untouched until phase end. Rename/merge to `main` is a phase-exit step.
- **Parallel-tab UI work (DO NOT TOUCH):** ~15 uncommitted working-tree files from another tab's Phase 1 dashboard polish (`page.tsx`, `layout.tsx`, `globals.css`, `QuickLogBar`, `TodayCard`, `FeedForm`, `DiaperForm`, `InsightBanner`, `ui/*`, `onboarding/page.tsx`, `log/*`). Every sub-agent stages ONLY its task's explicit files (`git add <paths>`, never `git add -A`/`.`). Tasks 2–3 have no overlap. **Task 4 collides on `TodayCard.tsx` + `(app)/layout.tsx`** — resolve parallel work before Task 4.
- [x] Task 3: Siri voice — bearer auth + API tokens — **COMPLETED** (commit `6f3b8e3`). `api-token.ts` mint/verify/revoke (verify via `withAdmin` pre-session conduit — no 3rd service-role file; mint/revoke RLS-bound); `with-auth.ts` bearer branch fail-closed (dead bearer ⇒ 401, no cookie fallback); `/api/voice` legacy adapter (server-side `client_uuid` fallback); `/api/tokens` + `/api/tokens/[id]`; `/settings/voice` + `VoiceTokensPanel`. `pnpm typecheck` + `pnpm lint` clean. Manual-only DoD outstanding: live `curl /api/events` fresh/wrong/revoked-token matrix + real-iPhone "Hey Siri" smoke (needs running server + device).
- [x] Task 4: Realtime + History + IntakeDonut + Pediatrician PDF — **COMPLETED** (commit `<PENDING>`). Shipped: `getRangeSummary` as a sibling of `getDaySummary` reusing one shared `reduceDay` per-day reducer (no drift); `/api/summary?days=N` (N=1 unchanged single payload, N>1 array); `/api/realtime-token` (5-min HS256 JWT via `jose`, signed with new `SUPABASE_JWT_SECRET` — NOT the service-role key, so the 2-file cap holds); `RealtimeProvider` (anon-key client + token refresh every 4 min, `router.refresh()` on feed/diaper `postgres_changes`, unmount cleanup, never blocks first paint); new `(app)/layout.tsx` mounting the provider (no redirect — pages keep self-gating); `IntakeDonut` (Recharts client island, nursing/pumped/formula + grey wasted arc, center total + target band) wired into TodayCard which stays a server component with its polish intact; `EventList` (grouped-by-day, logged-by/edited-by from `event_audit`, inline PATCH edit + confirm DELETE, optimistic UI); `/history` (7-day rollup via `getRangeSummary` + attribution); `PediatricianPDF` + `/api/export/pediatrician` (`renderToBuffer`, 1-page Letter, **structurally no `mom_events` / no caregiver attribution**, footer "Informational, not medical advice."); `/export/pediatrician` HTML preview + date-range picker; README + `.env.example` updated.
  - **Gate results:** `pnpm typecheck` clean (exit 0); `pnpm lint` clean (0 errors, exit 0); `pnpm build` succeeds (Compiled successfully, 36/36 pages); `pnpm test:integration` **23/23 passed** (no RLS regression — P0 gate held); unit suite 62/62.
  - **Env note:** new server-only `SUPABASE_JWT_SECRET` (Supabase Dashboard → Settings → API → JWT secret). Distinct from `SUPABASE_SERVICE_ROLE_KEY`; documented in `.env.example` + README. If unset the app still runs — Realtime just disables (devices stop auto-refreshing) and `/api/realtime-token` returns a clean 500 with a `fix_suggestion`.
  - **Manual-only DoD remaining:** two-phone live Realtime (<2s cross-device refresh); offline-does-not-break-dashboard smoke; iOS Safari iMessage-share of the PDF opening the iOS PDF viewer; visual/Lighthouse pass of the donut + history on a real device. (All require a running server + two devices — not automatable here.)
  - **Judgment call:** plan scopes the provider to `(app)/layout.tsx`; the root dashboard `/` (`src/app/page.tsx`) lives OUTSIDE the `(app)` group (owned by parallel UI work; not a Task 4 file) so it does not receive the socket from this layout — consistent with the existing root-layout comment that the `(app)` layout is the provider home. `/history` (inside the group) does get live updates. The two-phone *dashboard* check is a manual-only DoD item regardless.
