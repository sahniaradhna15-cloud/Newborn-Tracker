# Phase Progress: phase-3-pwa-offline-postpartum

**Plan:** `plans/phase-3-pwa-offline-postpartum.md`
**Reconciled:** 2026-05-18 — Phase 3 was built largely ahead-of-phase by a parallel tab; this session verified, integrated, validated, and committed it onto the de-facto phase branch `phase-2-task-1-integration-harness` (main untouched; rename/merge to `main` is the phase-exit step).

## Task Progress

- [x] Task 1: PWA installability — manifest, Serwist SW, icons, install prompt — **COMPLETED** (commit `86fc626`). `manifest.ts` (any + maskable icons); `sw.ts` Serwist policy: NetworkOnly for write/auth endpoints **incl. `/api/realtime-token`** (cross-task aware — a cached short-lived JWT would be stale), SWR `/api/summary`, CacheFirst `_next/static` + `/icons`, NetworkFirst pages (3s timeout); `next.config.ts` `withSerwist` (disabled in dev to avoid stale-cache HMR pain); generated `public/sw.js` git- + eslint-ignored; 4 calm placeholder icons (design refresh tracked post-MVP); root `layout.tsx` PWA metadata + `InstallPrompt` mounted **app-wide in the root layout** (Chromium `beforeinstallprompt` button + iOS-Safari "Add to Home Screen" sheet + standalone no-op). This root-layout mount also closes the Phase 3 Task 2 `OfflineBanner`/`QueueReplayProvider` mount that was deferred when `(app)/layout.tsx` did not yet exist.
- [x] Task 2: Offline queue + replay + offline-aware insight — **COMPLETED** (commit `61e8969`, pre-existing; mount completed in `86fc626`). IndexedDB write queue + sequential replay + `submitOrQueue`; `FeedForm`/`DiaperForm`/`QuickLogBar` enqueue-on-offline (via the `3d229ad` LogConsole consolidation); `OfflineBanner` + `QueueReplayProvider` now mounted app-wide.
- [x] Task 3: Settings (day-start / timezone / weight) + Growth chart with WHO percentiles — **COMPLETED** (commit `6458197`; WHO LMS math layer + tests pre-landed in `6264491`). `/growth` `GrowthChart` (Recharts) — P3/P15/P50/P85/P97 inverse-LMS bands + Anay's `weight_events`; header percentile via `weightPercentile` with below-1st/above-99th clamp. `/api/weights` POST (any caregiver, `client_uuid`-idempotent for offline replay) + PATCH/DELETE `[id]`; every weight write funnels through one `recomputeBabyCurrentWeight` helper so `babies.current_weight_oz` cannot drift (deleting newest reading rolls the cache back) and the TodayCard band recomputes. `/api/settings/household` (owner-only) + `/api/settings/baby` (split authority: owner for name/DOB/birth-weight, any caregiver for current weight via a `weight_events` insert). Settings page expanded to 4 cards + `SettingsForms`. `audit.ts` gains `weight.*` / `household.updated` / `baby.updated` kinds.
- [x] Task 4: Postpartum mom tab — **COMPLETED** (commit `f8e4d73`, pre-existing). Private mom logging + history; `mom_events_self` RLS keeps it partner-private.

### Gate results (full committed tree, 2026-05-18)
`pnpm typecheck` clean · `pnpm lint` clean · `pnpm test` (unit) **62/62** (incl. `who-growth.test.ts` WHO checkpoints) · `pnpm test:integration` **23/23** (P0 RLS cross-household isolation held — no Phase 1/2 regression) · `pnpm build` succeeds (Serwist SW bundled to `public/sw.js`, 36/36 pages).

### Known integration debt & manual-only DoD (carry forward — not blockers)
1. **Per-household `day_start_hour`/`timezone` not yet consumed by day-window math.** `/api/settings/household` persists them and audits the change, but `record-event.ts` / `day-summary.ts` still read the module-constant `America/Chicago` + hour 4. Honestly flagged in the route JSDoc; this is a coordinated shared-spine change (touches the DST-safe `day-window` core + its P0 tests) — do it as a deliberate follow-up, not a drive-by.
2. **~~Root dashboard `/` does not receive the Realtime socket.~~ RESOLVED 2026-05-19 (Option B).** The dashboard at `/` lives outside the `(app)` group, so `(app)/layout.tsx`'s `RealtimeProvider` never wrapped it — the main screen did not auto-sync across devices. Fixed without making the root layout async: extracted the layout's private `resolveActiveBabyId` into shared `src/lib/active-baby.ts`, reused it in both `(app)/layout.tsx` and `src/app/page.tsx`, and wrapped the dashboard JSX in `RealtimeProvider` (the dashboard already resolves session + redirects on no-session/no-baby, so no extra auth/passthrough logic is needed there; one extra RLS-bound indexed `babies` select). Provider renders children immediately + does socket work post-hydration, so first paint is unaffected; TodayCard stays an RSC. Gates: typecheck + lint clean. **Still a manual-only DoD:** the two-phone <2s cross-device dashboard refresh (needs a running server + two devices; also requires the Supabase `ALTER PUBLICATION` + `SUPABASE_JWT_SECRET` from item 3).
3. **Manual device DoD** (need a running server + real devices): Lighthouse PWA install audit; Chrome desktop/Android install + standalone launch; iOS "Add to Home Screen" sheet → standalone; airplane-mode enqueue→reconnect→drain; two-phone <2s Realtime; iOS PDF iMessage-share; `ALTER PUBLICATION supabase_realtime ADD TABLE feed_events, diaper_events;` must be run in Supabase (documented in README); set `SUPABASE_JWT_SECRET` (Supabase → Settings → API → JWT secret) for Realtime tokens.

### Orchestration note
Parallel-tab work continued to land ahead of phase (P2 T3, then P3 T1+T3+the LogConsole refactor) — the recurring pattern is **verify → integrate → validate → commit with strict explicit staging**, never re-implement. `.claude/agents/` is intentionally left untracked. Atomic commits this session: `6f6f283` (P1 polish), `875cfea` (plan reconcile), `42edb66`/`7a495a2` (P2 T4), `3d229ad` (LogConsole refactor), `86fc626` (P3 T1), `6458197` (P3 T3).
